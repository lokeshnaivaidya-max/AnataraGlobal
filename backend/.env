# Server Configuration
PORT=5001
NODE_ENV=development
DB_USER=postgres
DB_PASSWORD=Yashwanth%40567
DB_HOST=localhost
DB_NAME=antara
DB_PORT=5433

# Database Connection (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Change user, password, host, port, and database name as per your local PostgreSQL setup
DATABASE_URL="postgresql://postgres:Yashwanth%40567@localhost:5433/antara?schema=public"

# JWT Authentication
JWT_SECRET="antara-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# OTP Configuration
OTP_EXPIRES_IN_MINS=10

# SMTP Configuration (for Module 8 Email Notifications)
# Configure these with your real provider (e.g. SendGrid, Mailgun, or standard Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Antara <polepalliyashwanth@gmail.com>"

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5001/api/v1/auth/google/callback
FRONTEND_URL=http://localhost:5173